{
  "kind": "collectionType",
  "collectionName": "orders",
  "info": {
    "singularName": "order",
    "pluralName": "orders",
    "displayName": "order",
    "description": ""
  },
  "options": {
    "draftAndPublish": true
  },
  "pluginOptions": {},
  "attributes": {
    "order_id": {
      "type": "uid"
    },
    "order_date": {
      "type": "datetime"
    },
    "order_total": {
      "type": "decimal"
    },
    "order_address": {
      "type": "text"
    },
    "delivery_method": {
      "type": "enumeration",
      "enum": [
        "face-to-face",
        "delivery",
        "pick-up"
      ]
    },
    "order_status": {
      "type": "enumeration",
      "enum": [
        "pending",
        "in-process",
        "delivered",
        "dropped"
      ]
    },
    "order_products": {
      "type": "json"
    }
  }
}
